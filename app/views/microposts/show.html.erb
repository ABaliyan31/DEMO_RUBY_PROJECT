<p style="color: green"><%= notice %></p>

<div>
  <%= image_tag (@micropost.content), :width => "40%" %>

  </div>
<%= @micropost.comments.count %> comments
<%=  render @micropost.comments %>
<%= render partial: 'comments/form'%>
<div>
   <% user_id = @micropost.user_id %>
   <%  user_object = User.find(user_id) %>
  <%= link_to "Back to user of this post", user_path(user_object) %>

  <%# <%= button_to "Destroy this micropost", @micropost, method: :delete %>
</div>
